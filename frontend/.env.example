# Backend API base URL (no trailing slash). Browser-visible — set on Vercel for production.
# Production: https://anandam-life-1.onrender.com
# Local: http://localhost:4000
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Server-only admin login (set on Vercel for production)
ADMIN_USERNAME=
ADMIN_PASSWORD=
